# Example: 03-multi-agent-workflow

Orchestrator agent that delegates tasks to multiple specialist agents.

## Run

```bash
pnpm dev
```

The orchestrator will be available at `http://localhost:3003` (math agent on 3001, formatter on 3002).
