# Example: 02-task-streaming

SSE progress streaming demo. The agent performs a long-running task and emits live status updates.

## Run

```bash
pnpm dev
```

The agent will be available at `http://localhost:3005`.
