# Example: 04-mcp-bridge

A2A ↔ MCP bridge in both directions. Demonstrates an A2A agent calling MCP tools and an MCP host delegating to an A2A agent.

## Run

```bash
pnpm dev
```

The agent will be available at `http://localhost:3004`.
